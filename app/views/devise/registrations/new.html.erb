<h2 class="bg_sunflower">ユーザー登録</h2>
<div class="contents registration-block">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field"><div class="form">
   <dl>
<dt class="form-title"><%= f.label :メールアドレス %></dt>
    <dd class="form-item"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></dd>
</dl>
  </div>
  </div>

  <div class="field"><div class="form">
   <dl>
<dt class="form-title"> <%= f.label :パスワード %></dt>
    <dd class="form-item"><% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %></dd>
    <dd class="form-item"><%= f.password_field :password, autocomplete: "new-password" %></dd>
</dl>
  </div>
</div>

  <div class="field"><div class="form">
   <dl>
 <dt class="form-title"><%= f.label :パスワードの確認 %></dt>
   <dd class="form-item"> <%= f.password_field :password_confirmation, autocomplete: "new-password" %></dd>
</dl>
  </div>
  </div>

  <div class="actions">
    <%= f.submit "登録する" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
  </div>
